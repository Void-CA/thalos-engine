[package]
name = "thalos_ports"
version = "0.1.0"
edition = "2021"

[features]
default = []
test-support = []

[dependencies]
thalos-core = { path = "../thalos-core" }
serde = { version = "1.0", features = ["derive"] }
